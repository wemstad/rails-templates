- if current_user?
  #user-bar-greeting
    Logged in as
    = current_user.name
    (#{link_to "Log out", logout_path, { :title => "Log out" }})
- else
  #user-bar-greeting
    = link_to 'Log in', login_path, :style => 'border: none;'
